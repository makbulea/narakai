# Copy to .env and fill in real values. Never commit the real .env file.

# Npgsql connection string for the pgvector database started by docker-compose.yml.
RAG_POSTGRES_CONNECTIONSTRING=Host=localhost;Port=5433;Database=code_intelligence_rag;Username=rag;Password=rag

# Voyage AI API key (https://dashboard.voyageai.com/) — used by VoyageEmbeddingProvider.
VOYAGE_API_KEY=
